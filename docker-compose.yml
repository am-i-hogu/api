services:
  db:
    image: mysql:8.4
    container_name: am-i-hogu-db
    volumes:
      - mysql_data:/var/lib/mysql
    ports:
      - "3306:3306"
    environment:
      MYSQL_ROOT_PASSWORD: ${DB_ROOT_PASSWORD}
      MYSQL_DATABASE: ${DB_NAME}
volumes:
  mysql_data: